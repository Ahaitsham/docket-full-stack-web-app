# Leave EMPTY in local development (Vite proxies /api to http://localhost:5000).
# In production set it to your deployed backend URL, e.g. https://docket-api.vercel.app  (no trailing slash)
VITE_API_URL=
